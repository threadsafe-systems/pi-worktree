{
  "name": "pi-worktree",
  "version": "1.3.3-loom.8",
  "description": "Git worktree management for Pi (local fork). Adds: sibling worktree layout (<repoRoot>.worktrees/<branch-slug>), conventional-commit branch names, /worktree dispose (leave+remove, reopen in main repo), and session-forking relaunch so history follows every hop with a one-turn orientation caveat.",
  "license": "MIT",
  "type": "module",
  "keywords": [
    "pi",
    "pi-coding-agent",
    "pi-package",
    "git",
    "worktree",
    "cmux",
    "tmux"
  ],
  "files": [
    "extensions/worktree.ts",
    "skills/",
    "README.md",
    "FORK.md"
  ],
  "pi": {
    "extensions": [
      "./extensions/worktree.ts"
    ],
    "skills": [
      "./skills"
    ]
  },
  "scripts": {
    "lint": "npx tsc --noEmit"
  },
  "engines": {
    "node": ">=18"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-ai": "*",
    "typebox": "*"
  }
}
